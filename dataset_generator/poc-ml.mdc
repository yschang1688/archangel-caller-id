---
name: poc-ml
description: 數據導入機器學習的完整實戰流程 - 基於「全民瘋 AI 系列：經典機器學習」
---

# 數據導入機器學習：完整實戰流程

## 概述

本規則定義了從原始數據到機器學習模型部署的完整流程，選定適合的機器學習ML，適用於業界實戰專案。

---

## 階段一：資料探索與準備（第 2 章）

### 執行步驟

1. **資料載入與初步檢視** (2.2.4)
   - 使用 `pandas.read_csv()` 或對應方法載入資料
   - 檢查資料形狀：`df.shape`
   - 檢視前 5 筆資料：`df.head()`
   - 檢視資料型態：`df.dtypes`
   - 檢視基本統計：`df.describe()`

2. **探索式資料分析（EDA）** (2.2)
   - **直方圖** (2.2.5)：檢視數值特徵的分佈
   - **核密度估計圖** (2.2.6)：觀察連續變數的機率密度
   - **相關性熱圖** (2.2.7)：識別特徵間的相關性
   - **散佈圖** (2.2.8)：探索特徵與目標變數的關係
   - **盒鬚圖** (2.2.9)：識別離群值

3. **離群值檢查與處理** (2.3)
   - **檢查方法** (2.3.1)：
     - Z-score 方法（`scipy.stats.zscore`）
     - IQR 方法（四分位距）
     - 視覺化檢查（盒鬚圖）
   - **處理方法** (2.3.2)：
     - 刪除極端值（需謹慎，避免資訊損失）
     - Clipping（極端值平滑化，推薦）
     - 轉換（對數轉換、Box-Cox 轉換）

4. **缺失值處理** (2.4.1)
   - 檢查缺失值：`df.isnull().sum()`
   - 處理策略：
     - 數值型：均值/中位數填補
     - 類別型：眾數填補或新增「未知」類別
     - 缺失比例過高（>50%）：考慮刪除該特徵

5. **類別資料處理** (2.4.2)
   - **One-Hot Encoding**：適用於類別數較少（<10）
   - **Label Encoding**：適用於有序類別
   - **Target Encoding**：適用於高基數類別（推薦用 `category_encoders`）

6. **特徵縮放與轉換** (2.5)
   - **正規化 (Normalization)** (2.5.1)：將數值縮放到 [0, 1]
   - **標準化 (Standardization)** (2.5.2)：將數值轉換為均值 0、標準差 1
   - **RobustScaler**：對離群值更魯棒（推薦用於有離群值的資料）

### 輸出限制

- **資料品質檢查清單**：
  - [ ] 缺失值比例 < 20%（超過需特別處理）
  - [ ] 離群值已識別並處理
  - [ ] 類別特徵已適當編碼
  - [ ] 數值特徵已標準化/正規化
  - [ ] 特徵與目標變數的相關性已檢視

- **資料格式要求**：
  - 訓練集與測試集必須使用相同的預處理流程
  - 所有預處理器（Scaler, Encoder）必須保存以便推論時使用

### 結果品質檢查

- **資料完整性**：
  - 檢查處理後的資料是否有異常的 NaN 或 Inf 值
  - 確認資料型態正確（數值型為 float/int，類別型已編碼）

- **資料分佈檢查**：
  - 比較處理前後的資料分佈（使用直方圖）
  - 確認標準化後的資料均值接近 0、標準差接近 1

- **特徵工程合理性**：
  - 確認特徵數量合理（避免維度災難）
  - 檢查特徵間的共線性（相關性 < 0.9）

---

## 階段二：模型選擇與訓練（第 3-8 章）

### 執行步驟

1. **資料切分** (4.2.3, 4.4.3, 5.2.3, 7.4.3)
   - 使用 `train_test_split()` 切分訓練集與測試集
   - 推薦比例：80% 訓練集、20% 測試集
   - 分類問題使用 `stratify=y` 確保類別分佈一致

2. **Baseline 模型建立** (4.3)
   - **分類問題**：使用 `LogisticRegression` 作為基準
   - **迴歸問題**：使用 `LinearRegression` 作為基準
   - 目的：評估後續複雜模型的提升幅度

3. **模型選擇策略**

   **分類問題：**
   - **線性可分**：Logistic Regression (4.3) → SVM (6.2.1)
   - **非線性**：決策樹 (7.1) → 隨機森林 (8.2) → XGBoost (8.5)
   - **小樣本**：KNN (5.1) 或 SVM (6.1)

   **迴歸問題：**
   - **線性關係**：Linear Regression (4.1)
   - **非線性**：決策樹 (7.1) → 隨機森林 (8.2) → XGBoost (8.5)
   - **小樣本**：SVR (6.2.2)

   **非監督式學習：**
   - **分群**：K-means (3.2) 或 DBSCAN
   - **降維視覺化**：PCA (3.4.2) 或 t-SNE (3.4.3)

4. **模型訓練** (各章實務應用)
   - 使用訓練集訓練模型
   - 記錄訓練時間與資源使用

5. **超參數調整**（如適用）
   - 使用 `GridSearchCV` 或 `RandomSearchCV`
   - 配合交叉驗證（見階段三）

### 輸出限制

- **模型選擇原則**：
  - 優先選擇可解釋性高的模型（決策樹、線性模型）
  - 資料量 < 10,000：避免使用深度學習或過於複雜的模型
  - 不平衡資料：使用 `scale_pos_weight`（XGBoost）或 SMOTE（imbalanced-learn）

- **訓練時間限制**：
  - 單一模型訓練時間 < 30 分鐘（POC 階段）
  - 超參數搜尋總時間 < 2 小時

### 結果品質檢查

- **訓練過程檢查**：
  - 確認模型成功收斂（無錯誤訊息）
  - 檢查訓練集與驗證集的損失曲線（如有）
  - 確認無記憶體溢出或資源耗盡

- **模型合理性檢查**：
  - Baseline 模型表現符合預期（分類準確率 > 隨機猜測）
  - 複雜模型相較 Baseline 有明顯提升（F1-Score 提升 > 5%）

---

## 階段三：模型評估與驗證（第 9 章）

### 執行步驟

1. **交叉驗證** (9.2)
   - **K-Fold 交叉驗證** (9.2.2)：使用 `StratifiedKFold`（分類）或 `KFold`（迴歸）
   - 推薦 K=5 或 K=10
   - 計算平均分數與標準差

2. **評估指標選擇** (9.3.8, 9.3.9)

   **分類問題：**
   - **準確率 (Accuracy)**：僅適用於類別平衡的資料
   - **精準率 (Precision)**：關注誤判為正類的成本
   - **召回率 (Recall)**：關注漏抓的成本
   - **F1-Score**：平衡精準率與召回率（推薦）
   - **AUROC**：ROC 曲線下面積
   - **AUPRC**：Precision-Recall 曲線下面積（不平衡資料推薦）

   **迴歸問題：**
   - **MAE (Mean Absolute Error)**：平均絕對誤差
   - **RMSE (Root Mean Squared Error)**：均方根誤差
   - **R² Score**：決定係數（需配合其他指標使用）

3. **過擬合檢查** (9.1)
   - 比較訓練集與測試集的表現
   - 訓練集表現 >> 測試集表現 → 過擬合
   - 訓練集與測試集表現都差 → 欠擬合

4. **模型可解釋性** (7.3.1, 8.3.6, 8.6.7)
   - **特徵重要性**：使用 `feature_importances_`（樹模型）
   - **SHAP 值**：使用 `shap` 套件進行深度解釋（推薦）
   - **決策路徑**：視覺化決策樹的決策過程

### 輸出限制

- **評估指標要求**：
  - 分類問題：必須報告 Precision, Recall, F1-Score（至少三項）
  - 迴歸問題：必須報告 MAE, RMSE, R²（至少三項）
  - 禁止僅使用 Accuracy 或 R² 單一指標評估

- **交叉驗證要求**：
  - 必須執行至少 5-Fold 交叉驗證
  - 報告平均分數與標準差（格式：`0.85 ± 0.03`）

- **過擬合檢查要求**：
  - 訓練集與測試集 F1-Score 差異 < 0.1（分類）
  - 訓練集與測試集 RMSE 差異 < 10%（迴歸）

### 結果品質檢查

- **模型表現合理性**：
  - 分類問題：F1-Score > 0.7（或根據業務需求調整）
  - 迴歸問題：R² > 0.6 且 RMSE 在可接受範圍內

- **穩定性檢查**：
  - 交叉驗證標準差 < 0.05（表示模型穩定）
  - 不同隨機種子下的表現差異 < 5%

- **可解釋性檢查**：
  - 特徵重要性前 5 名符合業務直覺
  - SHAP 值能解釋關鍵預測案例

---

## 階段四：模型部署與整合（第 10 章）

### 執行步驟

1. **模型序列化** (10.2)
   - 使用 `joblib.dump()` 儲存模型
   - 儲存完整的 Pipeline（包含預處理器）
   - 記錄模型版本與訓練日期

2. **API 服務化** (10.4)
   - 使用 FastAPI 建立推論服務
   - 實作 `/predict` 端點
   - 實作 `/health` 端點（健康檢查）
   - 自動生成 API 文件

3. **前後端整合** (10.5)
   - 建立簡單的前端界面（可選）
   - 串接 API 進行即時推論

### 輸出限制

- **模型檔案要求**：
  - 模型檔案大小 < 500MB（過大需考慮模型壓縮）
  - 必須包含完整的預處理 Pipeline
  - 必須包含模型版本資訊

- **API 效能要求**：
  - 單次推論時間 < 1 秒（POC 階段）
  - API 回應時間 < 500ms（生產環境）

- **API 文件要求**：
  - 必須自動生成 OpenAPI/Swagger 文件
  - 包含請求/回應範例

### 結果品質檢查

- **模型載入測試**：
  - 確認模型可正常載入
  - 確認推論結果與訓練時一致

- **API 功能測試**：
  - 測試正常請求（200 狀態碼）
  - 測試異常請求（400/500 錯誤處理）
  - 測試邊界值（極端輸入）

- **資料一致性檢查**：
  - 確認推論時的資料預處理與訓練時一致
  - 確認特徵順序與數量正確

---

## 常見錯誤與避免方法（第 9.3 章）

### 必須避免的錯誤

1. **資料收集與處理不當** (9.3.1)
   - ❌ 未檢查資料品質就開始訓練
   - ✅ 執行完整的 EDA 與資料清理

2. **訓練集與測試集的類別分佈不一致** (9.3.2)
   - ❌ 未使用 `stratify` 參數
   - ✅ 使用 `stratify=y` 確保分佈一致

3. **沒有資料視覺化的習慣** (9.3.3)
   - ❌ 僅看數值統計
   - ✅ 使用視覺化發現資料模式

4. **使用錯誤方法為特徵編碼** (9.3.4)
   - ❌ 對有序類別使用 One-Hot Encoding
   - ✅ 根據類別特性選擇適當編碼方法

5. **資料處理不當導致資料洩漏** (9.3.5)
   - ❌ 在切分資料前進行標準化
   - ✅ 先切分資料，再分別對訓練集與測試集進行預處理

6. **僅使用測試集評估模型好壞** (9.3.6)
   - ❌ 僅報告測試集分數
   - ✅ 使用交叉驗證評估模型穩定性

7. **在沒有交叉驗證的情況下判斷模型性能** (9.3.7)
   - ❌ 僅使用單次 train_test_split
   - ✅ 執行至少 5-Fold 交叉驗證

8. **分類問題僅使用準確率作為衡量模型的指標** (9.3.8)
   - ❌ 僅報告 Accuracy
   - ✅ 報告 Precision, Recall, F1-Score, AUPRC

9. **迴歸問題僅使用 R2 分數評估模型好壞** (9.3.9)
   - ❌ 僅報告 R²
   - ✅ 報告 MAE, RMSE, R²

10. **任何事情別急著想用 AI 解決** (9.3.10)
    - ❌ 過度複雜化簡單問題
    - ✅ 評估是否真的需要機器學習

---

## 實戰檢查清單

### 資料準備階段
- [ ] 完成 EDA（至少包含直方圖、相關性熱圖、散佈圖）
- [ ] 處理缺失值（缺失比例 < 20%）
- [ ] 處理離群值（使用 Clipping 或轉換）
- [ ] 類別特徵已適當編碼
- [ ] 數值特徵已標準化/正規化
- [ ] 資料切分前後分佈一致（使用 `stratify`）

### 模型訓練階段
- [ ] 建立 Baseline 模型
- [ ] 選擇至少 2-3 個候選模型
- [ ] 執行超參數調整（如適用）
- [ ] 記錄訓練時間與資源使用

### 模型評估階段
- [ ] 執行至少 5-Fold 交叉驗證
- [ ] 報告多個評估指標（至少 3 個）
- [ ] 檢查過擬合/欠擬合
- [ ] 分析特徵重要性或 SHAP 值
- [ ] 比較 Baseline 與最終模型表現

### 模型部署階段
- [ ] 儲存完整的 Pipeline（包含預處理）
- [ ] 建立 FastAPI 服務
- [ ] 測試 API 功能與效能
- [ ] 生成 API 文件

---

## 參考資源

- **教材**：《全民瘋 AI 系列：經典機器學習》
- **環境**：fraudml conda 環境（Python 3.11）
- **核心套件**：pandas, numpy, scikit-learn, xgboost, shap, imbalanced-learn, FastAPI

---

**最後更新**：2025-01-14
